# Plane project configuration

Template for `.claude/plane-workflow.md` in a project root. Initialization fills
this in from live Plane data — see [initialization.md](initialization.md). Do not
hand-write the IDs; they must come from the API or they will be wrong.

Copy everything below the line.

---

# Plane configuration

<!-- Generated by the plane-workflow skill. Re-run initialization after changing
     project states, and commit this file — it is team configuration, not a secret. -->

| Field | Value |
| ----- | ----- |
| Project name | |
| `project_id` | |
| Ticket identifier | |
| `workspace_slug` | `server-configured` — omit from calls, or the literal slug |
| MCP tool prefix | e.g. `mcp__plane__` |

## State roles

Every state the lifecycle refers to, bound to this project's real states. Roles
with no matching state are recorded as `none` — the lifecycle skips them.

| Role | State name | `state_id` | Group |
| ---- | ---------- | ---------- | ----- |
| `start` — set when work begins | | | |
| `review` — set at hand-off, the agent's last state change | | | |
| `plan` — where parent/feature items are created | | | |

## Ship states — never set by the agent

States that a deploy, release, or another person sets after hand-off.

| State name | `state_id` | Group |
| ---------- | ---------- | ----- |
| | | |

## All states

Full list, for moves the lifecycle doesn't cover.

| State name | `state_id` | Group |
| ---------- | ---------- | ----- |
| | | |

## Verification gates and branch flow

Not recorded here. They live in `.claude/shipping-changes.md`, owned by the
`shipping-changes` skill, because they are git concerns rather than tracker
concerns and both skills need the same answer.

If that file doesn't exist, initialize `shipping-changes` first. If the project
doesn't use that skill at all, record the gate commands and the integration
branch below and say so, so the next session doesn't go looking.

| Field | Value |
| ----- | ----- |
| Shipping config | `.claude/shipping-changes.md`, or `not used — see below` |

## Labels

Labels defined on this project. If the table is empty, the project has none —
**do not invent them.**

| Label | `label_id` |
| ----- | ---------- |
| | |

## Sibling projects — do not file here

Other projects in this workspace, recorded so tickets don't land in the wrong one.

| Project | Identifier |
| ------- | ---------- |
| | |
